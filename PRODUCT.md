# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

面向处理项目相关业务的电网用户。财务/预算专职、项目与业务经办、审批人及管理查看角色并存；用户通过菜单与按钮权限进入各自职责范围，完成预算/项目过程中的查询、编制、配置、审批与监控，而不是单一角色的专用工具。

## Product Purpose

**项目过程管控平台**是电网侧的综合运营中台（工程名为 `budget-process`）。目标是把预算过程、项目过程、物资与指标、智能审核与规则配置等能力收敛到统一 Web 后台，让组织在一套入口里完成跨域业务操作与管控。

成功表现为：有权限的用户能在正确组织范围内完成任务，配置与数据口径一致，关键业务结果可追溯。

## Positioning

统一入口的运营中台：权限、字典与范围配置驱动的多业务域协作，而不是单一预算表单系统或独立 AI 工具。相邻产品若只做预算编制、只做项目台账或只做审核规则，无法完整替代其「按角色贯通多域过程」的定位。

## Operating Context

- 桌面浏览器内的管理后台；登录后按角色/单位/部门加载菜单与按钮。
- 典型工作流：选组织或项目类型 → 列表查询 → 编辑/批量配置 → 弹窗确认 → 保存回刷。
- 常见业务对象：项目类型树、一级单位、启用范围、审核规则、外包负面清单、提取 Schema、工作流待办等。
- 开发与联调可走 Vite 本地服务；网关与服务映射见 `public/config.json`（如 budget-process 服务）。

## Capabilities and Constraints

**已确认能力（代码与菜单结构可证）：**

- 多业务域视图：预算过程、项目信息、工作流、物资/指标、苏州等区域模块、AI 智能审核相关配置与任务。
- 权限：`userDialog` / 角色 busicode / 按钮权限（如 EDIT、DELETE）控制可见可操作范围。
- 配置驱动：公共代码字典、页签、启用类型、规则分类等由后台字典提供，前端少硬编码业务枚举。
- 列表与表单范式：ProTable、弹窗编辑、树选项目类型、批量操作与确认。

**硬约束（用户确认）：**

- 权限与角色隔离必须始终成立。
- 仅面向桌面 Web 后台，遵循 Element Plus 管理端交互预期，不扩展手机端产品范围。
- 提示框必须使用正式、准确、与当前操作及结果一致的提示信息；不得使用含糊、口语化或与业务无关的提示文案。
- 国网/电力业务术语（项目类型、一级单位、可研/评审阶段等）不可随意改写。
- 可配置优先，避免把字典项、范围与规则写死在前端。

**技术事实：**

- Vue 3 + Vue Router + Vuex + Element Plus + vxe-table + Vite。
- 中文界面（默认 `zh-CN`）。
- 主题主色在既有样式中为青绿系（如 `#0e8b8d` / 页面级 `#00857c` 变量），init 不规定视觉世界，仅作现有实现事实。

**未决（不编造）：**

- 无障碍合规等级（如 WCAG 目标）未单独确认。
- 各业务域的优先级与路线图未在本记录中锁定。

## Brand Commitments

- 正式产品名：**项目过程管控平台**；对内工程名为 `budget-process`。
- 运行环境域名与组织语境指向国网江苏相关体系（如 `*.js.sgcc.com.cn`），设计与文案需保持政务/国企管理端稳重、可操作，不引入消费级营销话术。
- Logo 默认未启用（`enabledLogo: false`）；无强制对外 slogan。

## Evidence on Hand

- **视觉标杆页：** `src/views/suzhou/aiAuditInuseRange`（DESIGN.md 权威实现；新业务页应对齐）。

- 代码与模块：`src/views/**`（ai、budget-process、workflow、suzhou、service 等）、`src/api/**`、`src/components/ProTablePage`。
- 配置：`public/config.json`、`package.json`（name: budget-process）。
- README 操作规范：查询区「查询/重置」、主次/危险按钮语义约定。
- Mock 与角色样例：如财务预算专职、项目类型树、AI 审核启用范围。
- **不得编造**：客户证言、上线规模、行业排名、未在仓库出现的对外品牌资产。

## Product Principles

1. **权限先于界面**：任何新能力先落到角色/按钮/数据范围，再谈布局与样式。
2. **配置驱动业务**：字典、页签、范围与规则来自后端配置，前端表达配置结果而非固化业务表。
3. **术语忠实**：沿用国网/电力与预算项目过程既有称谓，不为「好读」擅自替换关键名词。
4. **操作可完成**：列表—选择—编辑/批量—确认—回刷是默认路径；危险操作需明确确认。
5. **中台一体、分域清晰**：统一壳与导航下保持各业务域信息架构清楚，避免做成营销落地页。

## Accessibility & Inclusion

未单独约定合规等级。默认按桌面管理端可用性处理：可键盘聚焦的关键控件、清晰禁用态、错误与空状态需可理解。若后续有政务无障碍要求，再升级本段。
